const pathnames: {
  [key: string]: string
} = {
  signIn: '/sign-in',
  signUp: '/sign-up',
  changePassword: '/change-password',
  chats: '/chats',
  userForm: '/user-form',
};

export default pathnames;
