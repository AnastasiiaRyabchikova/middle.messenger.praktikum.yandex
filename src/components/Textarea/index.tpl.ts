const template: string = `
  <textarea
    class="{{class}}"
    type="{{type}}"
    placeholder="{{placeholder}}"
    name="{{name}}"
    class="{{class}}"
    value="{{value}}"
    required="{{required}}"
  />
`;

export default template;
