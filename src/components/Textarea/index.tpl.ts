const template: string = `
  <textarea
    class="{{class}}"
    placeholder="{{placeholder}}"
    name="{{name}}"
    class="{{class}}"
    value="{{value}}"
    required="{{required}}"
  />
`;

export default template;
